export declare const formatDate: (d: Date) => `${number}-${number}-${number}`;
export declare const getFutureDate: (d: Date, addDays: number) => Date;
export declare const normalizeDate: (d: Date) => string;
